{% set title = 'Basic Website' %}

{% set introduction %}
Tina4 implements TWIG templating out of the box so all your html files should end with the twig extension.  Using twig allows you to use includes and therefore simplifies implementing web pages.
{% endset %}

{% set requirements = ['HTML', 'Twig'] %}

{% set content %}
Under <code>src/templates</code> create a file called <code>index.twig</code>
<pre><code>{{ include_code("examples/basic-website.twig"| raw) }}</code></pre>
Click on the <a href="http://localhost:7145">http://localhost:7145</a> to see the site, once you have replaced the default site you can get your documentation here <a href="http://localhost:7145/documentation">http://localhost:7145/documentation</a>
{% endset %}

{% set tips = [
    'Tina4 templates are located under <code>src/templates</code>',
    'Routing is created dynamically, for example a file called <code>src/templates/contact-us.twig</code> will be accessible at <a href="/contact-us">http://localhost:7145/contact-us</a>',
    'Use <code>{% include "filename.twig" %}</code> to include a file, the include path is relative to the <code>src/templates</code> folder'
]
%}

{% include "documentation/components/help-segment.twig" %}